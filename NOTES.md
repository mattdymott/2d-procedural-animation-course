# Teaching Notes

## User preferences
- Very comfortable with DOTS (ISystem, Burst, jobs) — never explain ECS plumbing; frame recipes as "here's the DOTS mapping" sidebars.
- Math depth: intuition + working recipes. No derivations. Geometric/visual explanations land best.
- Mission is triple-headed: Rain World creatures + juice-at-scale + reusable toolkit package. When choosing the next lesson, prefer techniques that serve all three.

## Working notes
- User moved the course into `Assets/Breakdowns/2D Procedural Animation with DOTS/` (2026-07-14) — ALWAYS write course files there, not the Breakdowns root. Unity generates .meta files; harmless.
- User has an extensive library of game-mechanics breakdowns in the parent folder (Rain World, Animal Well, etc.) — good hooks for grounding examples.
- Curriculum spine (proposed 2026-07-14): verlet chains → muscles → analytic 2-bone IK → FABRIK → gait/stepping → second-order dynamics (juice) → DOTS batching patterns → package API design. (User chose the creature path at the L1 branch; juice deferred, not dropped.)
- Lesson 0001: verlet integration + distance constraints, interactive rope sim. Completed.
- Lesson 0002: muscles (goal = weak pin, rhythm = traveling wave with envelope, tone = posture springs), tentacle-hunts-firefly sim. Challenge thresholds validated headlessly in Python (scratchpad tentacle_check.py pattern — replicate sim, tune before shipping). Completed.
- Lesson 0003: analytic 2-bone IK, position form (circle-intersection intuition, annulus clamp, bendSign). Playground draws the construction circles + ghost of the other bend; challenges: dead disk + knee-vs-wall. Solver validated to 1e-13 over 100k random cases. Course shorthand established: muscles = wishes, IK = promises.
- Lesson 0003 completed. Lesson 0004: gait/stepping (event-driven: comfort-zone trigger + partner guard + velocity-lead targets + parabolic swing), two-legged walker sim with chase/patrol modes. Gait rules validated headlessly incl. patrol turnarounds (isolated 1-frame strain at flips is natural — challenge 2 discriminates on sustained strain runs, not zero strain). Course now covers a complete walker; "graduation build" rep assigned (full creature: chain body + muscles + IK legs + gait).
- Lesson 0004 completed. Lesson 0005: FABRIK (backward/forward passes = L1 sweeps with intent; warm starting; unreachable → straighten; grab muscle = blend toward solution pose, the wish⇄promise dial). Tail-grabs-firefly sim; validated headlessly: warm-started 1-iter tracking at 0.18px avg error, early-out averages 1.24 iters at cap 10. Solver set now complete (analytic 2-bone + FABRIK).
- Lesson 0005 completed. Lesson 0006: second-order dynamics (t3ssel8r filter — f/ζ/r knobs, stability-clamped semi-implicit update, r<0 = anticipation, yd drives squash for free). Blob-chases-target sim with jump mode; math validated headlessly stable incl. f=4/ζ=0 stress (no blowup), z=0.4→24% overshoot, r=-2→56% anticipation, baseline z=1/r=0→zero of each. First lesson of the juice/toolkit branch.
- Lesson 0006 completed. Lesson 0007: batching at scale — first synthesis lesson (no new technique). Five animation-specific patterns (parallelize-across / self-contained / enableable-not-structural / buffer-in-chunk / branchless) + simulation LOD as the multiplier. Interactive frame-BUDGET sandbox (cost model, not real perf — reproducible): count/near-fidelity/near-band/far-divisor sliders vs 16.6ms bar. Model validated in budget_check.py: 15k@8iter no-LOD = 21.6ms (the wall), challenges force LOD. Current-API claims (enableable, InternalBufferCapacity) grounded in official docs, added to RESOURCES.
- Lesson 0007 completed. Lesson 0008: package API design (SPINE COMPLETE). Composition-as-API, deep modules (Ousterhout, cited), config-baked-vs-state-separate, two-layer+escape-hatch, glossary-as-vocabulary, progressive disclosure. Interactive = "compose a creature" builder (toggle package components to satisfy 4 creature briefs; validates deps + brief fit). Logic validated in compose_check.py (briefs consistent + mutually distinct). Sources cited: APoSD, Unity Baker docs.
- SPINE COMPLETE (8/8). All three mission heads served. No default next lesson — future sessions are branches/depth. Offer options: guided end-to-end capstone build (full lizard), joint limits & collision, authoring-time creature editor tooling, or deepening any technique. Ask the user which; don't assume.
- Lesson 0009 started as the guided capstone branch (2026-08-06): the new skill is orchestration, not a ninth solver. It assembles the walking lizard through an explicit authority-preserving tick: intent → body wishes → gait promises → IK → hard constraint resolve → presentation. Do not retroactively call it part of the eight-lesson spine.
- Lesson 0010 continues the creature-depth branch (2026-08-06): contact and joint limits are one-sided temporary constraints, interleaved with hard distance sweeps. The single invariant is: contacts push out, never pull in; presentation cannot repair penetrations.
- Lesson 0011 continues the creature-depth branch (2026-08-06): terrain is sampled only when a gait foot begins swing, then filtered by support normal and reach before the resulting target becomes a planted promise. Never re-query beneath an already planted foot.
- Lesson 0012 continues the creature-depth branch (2026-08-06): a foot planted on a moving support stores its support entity and support-local point. Convert it to world each tick; never freeze an old world point or chase a fresh raycast.
- Lesson 0013 continues the creature-depth branch (2026-08-06): distinguish a support pose moving coordinates from conveyor material moving through those coordinates. A planted conveyor foot carries an integrated support-local surface offset; liftoff inherits support-point velocity plus surface velocity.
- Lesson 0014 continues the creature-depth branch (2026-08-06): liftoff from a rotating support inherits velocity at the committed contact point, not root velocity. In 2D this is linear velocity plus angular velocity times the perpendicular radius; conveyor surface travel remains a separate term.
- Lesson 0015 starts the authoring-tools branch (2026-08-06): serialized creature blueprint fields are the one authored truth; scene preview geometry is derived on repaint; mutable simulation state is initialized and evolved only at runtime. Possible next authoring depth: preflight validation of impossible limb/gait settings.
- Lesson 0016 starts the top-down locomotion branch (2026-08-08): retain planted/swinging promises and velocity lead, but replace flat ground/down with an explicit movement plane and heading-rotated local foot homes. Turning changes plant stress; visual foot lift is presentation over the planar trajectory. Likely depths: path-aware legal footholds, turn/support-polygon coordination, and readable top-down swing presentation.
- Lesson 0017 continues the top-down locomotion branch (2026-08-08): planar queries supply walkability and path-clear facts at swing start; gait filters candidates by those facts, reach, and policy, then commits one target. A blocked obstacle is not a support-normal problem, and refreshed sensing never overwrites a plant.
- Lesson 0018 continues the top-down locomotion branch (2026-08-08): turning makes heading-relative homes stress planted legs, but support policy decides whether a stressed leg may lift. Begin with the smallest rule that reads well (partner guard, then minimum planted feet); only add a geometric support region when the actual creature needs it.
- Lessons need a localhost server for the in-app Browser pane (blocks file:// and pauses rAF when hidden) — `python -m http.server 8734` from the Breakdowns root; or just open the file in a real browser.
