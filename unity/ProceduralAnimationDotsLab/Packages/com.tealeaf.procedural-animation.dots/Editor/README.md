# Editor

The creature Baker, validation, and derived Scene-view preview belong here.
`ProceduralCreatureAuthoring` itself is runtime-facing so designers can attach
it to a GameObject; this module serializes it into package-owned runtime state.
