# Procedural Animation DOTS Lab sample

The existing lab will become this package's first sample. Its deterministic
terrain query, animated support, patrol input, and line rendering are sample
adapters rather than Runtime dependencies.
